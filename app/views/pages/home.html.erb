  <div class="background-image">
  <%# <%= image_tag('landing_page.png.PNG') %> 
  <div>
    <h1>Welcome to CabInn</h1>
    <% if user_signed_in? %>
      <h3>Hi <%= current_user.username.capitalize %></h3>
    <% end %>

    <div class="button-index">
      <%= link_to "See all available cabins", flats_path, class: "btn btn-primary" %>
    </div>
  </div>

  </div>
